<div class="col-md-2">
<h2><%= link_to 'Create Patient', new_patient_path,:class => "btn btn-default btn-100" %></h2>
</div>
<div class="col-md-10">
<table class="table table-condensed">
	<thead>
		<tr>
			<th>Patient</th>
			<th>Family Group</th>
			<th></th>
		</tr>
	</thead>
	<tbody>
	    <% @patients.each do |patient| %>
	      <tr>
	        <td><%= link_to patient.fullname, patient %></td>
	        <td><%= FamilyGroups(patient.family_groups) %></td>
	      </tr>
	    <% end %>
	</tbody>
</table>
</div>

