<!DOCTYPE html>
<html>
	<head>
	  <title>Healthcenter</title>
	  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
	  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
	  <%= csrf_meta_tags %>
	  <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
	</head>
	<body>
		<% if current_user %>







<div class="navbar navbar-inverse navbar-fixed-top nav-justified">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle Navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <% if current_user.is_admin %>
        <span class="icon-bar"></span>
					<% end %>
      </button>
     <!-- <a class="navbar-brand"
      	<%= link_to "Usuario: "+ current_user.firstname, user_path(current_user) %>
      ></a>-->


    </div> <!-- end div class="navbar-header" -->
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <li><%= link_to "Inicio",home_welcome_path %></li>
        <li><%= link_to "Pacientes", patients_path %></li>
        <li><%= link_to "Grupos Familiares", family_groups_path %></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
      	<% if current_user.is_admin %>
						<li><%= nav_link "Crear Usuarios", new_user_path%></li>
					<% end %>
				  	
					<li><%= link_to session_path, method: :delete do %>
						<i class="glyphicon glyphicon-log-out"></i>
						<% end %></li>
					<li><%= link_to users_path do %>
						<i class="glyphicon glyphicon-cog"></i>
						<% end %></li>
				  </ul>
    </div> <!-- end div class="collapse navbar-collapse" -->
  </div> <!-- end div class="container" -->
</div> <!-- end div class="navbar navbar-default navbar-fixed-top" -->













<!--
			<div class="navbar navbar-fixed-top navbar-inverse" role="navigation">
				<div class="container">
				</div>
				<div class="navbar-collapse collapse">
				  <ul class="nav navbar-nav">
				    <%= nav_link "Inicio", home_welcome_path%>
					<%= nav_link "Grupos Familiares", family_groups_path%>
					<%= nav_link "Pacientes", patients_path%>
					<%= nav_link "Equipo", users_path%>
					<% if current_user.is_admin %>
						<%= nav_link "Crear Usuarios", new_user_path%>
					<% end %>
				  </ul>
				  <ul class="nav navbar-nav navbar-right">
				  	<% if !current_user.firstname.nil? %>
				  		<li> <%= link_to "Usuario: "+ current_user.firstname, user_path(current_user)%></li>
				  	<% else %>
				  		<li> <%= link_to "Usuario: "+ current_user.email, user_path(current_user)%></li>
				  	<% end %>
				 	
				  	<li><%= link_to 'Inbox', inbox_path(current_user) %></li>
					<li><%= link_to 'Cerrar sesión', session_path, method: :delete %></li>
				  </ul>
				</div>	
			</div>

-->



		<%end%>
		<%= yield %>

	</body>
</html>
